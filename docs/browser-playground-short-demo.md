# Short screen demo

A ~10s recording of the Browser Playground app:

1. Increment the Home counter (+ twice)
2. Toggle light/dark theme

The demo video is attached on the pull request (cloud agent artifact).
